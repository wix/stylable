import "../../commons/design-system.st.css.mjs";
import { classes } from "./label.st.css.mjs";

const render = (text = '') => {
  const label = document.createElement("label");
  label.textContent = text;
  label.classList.add(classes.root);
  return label;
};

export { render };
