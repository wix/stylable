import { classes } from "./label.st.css.js";

const render = (text = '') => {
  const label = document.createElement("label");
  label.textContent = text;
  label.classList.add(classes.root);
  return label;
};

export { render };
