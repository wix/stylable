import style from './badge.st.css';
const render = (_text = 'Badge') => {
    const lbl = document.createElement('badge');
    lbl.textContent = _text;
    lbl.classList.add(style.root);
    return lbl;
};
export { style, render };
