import classes from "./text.st.css";
const render = (_text = 'Text') => {
  const lbl = document.createElement("text");
  lbl.textContent = _text;
  lbl.classList.add(classes.root);
  return lbl;
};
export { classes, render };
