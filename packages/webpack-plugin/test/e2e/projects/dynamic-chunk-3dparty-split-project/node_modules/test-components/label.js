import style from "./label.st.css";
const render = (_text = 'Lable') => {
  const lbl = document.createElement("label");
  lbl.textContent = _text;
  lbl.classList.add(style.root);
  return lbl;
};
export { style, render };
