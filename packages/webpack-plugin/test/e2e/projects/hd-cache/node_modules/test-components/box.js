import { classes } from "./box.st.css";
const render = (_text = 'Button') => {
  const box = document.createElement("div");
  box.classList.add(classes.root);
  return box;
};
export { classes, render };
